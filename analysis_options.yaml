# 静态分析配置（大型团队协作基线）。
# 基础规则来自 flutter_lints；下方在其之上补充了一批面向生产的规则，
# 用于统一代码风格、防止常见性能/正确性问题，并强化架构约束。
include: package:flutter_lints/flutter.yaml

analyzer:
  # 生成产物与预览入口不参与团队规范校验。
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "build/**"
    - ".dart_tool/**"

  # 把易引发线上问题的项目从「提示」升级为「错误」，CI 直接拦截。
  errors:
    # 未处理的 Future 极易造成竞态/丢异常，交付前必须清零。
    unawaited_futures: error
    # 跨 await 使用 BuildContext 是 Flutter 常见崩溃源。
    use_build_context_synchronously: error
    # 生产代码禁止 print，统一走日志。
    avoid_print: error
    # 未使用的导入/变量应清理，避免腐化。
    unused_import: warning
    unused_local_variable: warning

linter:
  rules:
    # —— 正确性 / 稳定性 ——
    always_use_package_imports: false # 本项目 feature 内部使用相对导入，保持现状
    avoid_print: true
    unawaited_futures: true
    use_build_context_synchronously: true
    cancel_subscriptions: true
    close_sinks: true
    avoid_empty_else: true
    avoid_relative_lib_imports: true

    # —— 不可变 / 性能 ——
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    use_key_in_widget_constructors: true
    sized_box_for_whitespace: true
    avoid_unnecessary_containers: true
    prefer_final_locals: true
    prefer_final_in_for_each: true

    # —— 可读性 / 一致性 ——
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_child_properties_last: true
    unnecessary_brace_in_string_interps: true
    unnecessary_underscores: true
    prefer_relative_imports: false
    curly_braces_in_flow_control_structures: true
    sort_constructors_first: true

    # —— API / 契约 ——
    always_declare_return_types: true
    avoid_returning_null_for_void: true
