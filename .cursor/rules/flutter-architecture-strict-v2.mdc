---
description: Flutter Feature-First 架构强制规范 (STRICT V2) — 分层、依赖、UI、状态、路由
alwaysApply: true
---

# Flutter Project Development Rules (STRICT V2)

## 0. 核心目标

- 长期可维护
- 可扩展而不重构
- UI / 业务 / 数据彻底解耦
- 避免隐性依赖

## 0.1 设计规范权威（最高优先级 · 强制）

`design-system/` 文件夹是本项目 UI 的**最高优先级唯一权威**。任何改动只要涉及
**颜色 / 字号 / 行高 / 字重 / 间距 / 圆角 / 组件外观或变体**，**动手前必须先对照** `design-system/`，与其冲突时**以 `design-system/` 为准**。

权威文件：

| 文件 | 角色 |
|------|------|
| `design-system/README.md` | **文本权威基线**（token + 组件规范），冲突时以此为准 |
| `design-system/design-system-spec.canvas.tsx` | 可视化目录 / 预览（源文件，随仓库版本管理） |

**强制规则：**

1. **先对照后动手**：涉及上述维度的改动，先读 `design-system/README.md` 找到对应 token / 组件档位，直接引用，禁止另造。
2. **改源即全局**：token 真源在 `lib/core/theme/*`，组件真源在 `lib/shared/*`；只改这一处，全局同步。禁止在页面内重定义样式或复制组件。
3. **新增即询问**：任何超出规范的新 token / 新值 / 新档位 / 新色系 / 新组件变体，**必须停止**，先向用户说明用途与建议值并取得确认，落地后**同步更新 `design-system/README.md`**。收敛去重（把重复字面量指向已有 token、值不变）不算新增，可直接做。
4. **三处一致（强制）**：`design-system/README.md`（文本）、`design-system/design-system-spec.canvas.tsx`（源）、Cursor 托管渲染副本 `~/.cursor/projects/<workspace>/canvases/design-system-spec.canvas.tsx` 三者内容必须一致。**每次修改可视化文档后，必须把源文件同步到托管副本**（见 `.cursor/skills/flutter-post-edit-audit`：`scripts/sync-canvas.sh`），否则预览不会更新。
5. 若同时改了 `lib/`（组件 / token）与 `design-system/`，两侧描述必须对齐。
6. **三主题默认同步（强制）**：用户未特别声明时，凡涉及 UI 视觉 / token / 组件外观的修改，**必须同时覆盖三个编译期主题包** `yellow_dark`（黄黑 · 默认）、`pink_light`（浅粉）、`yellow_light`（黄浅）。不得只修一个浅色包或只验当前预览主题。实现方式：改 `lib/core/theme/*` 语义 token 或 `lib/shared/*` 组件真源，让三主题经既有分支（`_isLight` / `themeId` / keep-dark）正确解析；若某主题需刻意例外（如 keep-dark 浮层），须在代码注释与 `design-system/README.md` 登记，并在交付说明中写明。

## 1. 架构强制（Feature First）

```
lib/
  core/
  shared/
  features/
  routes/
```

### 1.1 feature 标准结构

```
features/<name>/
  data/
  domain/
  application/
  presentation/
```

### 1.2 禁止跨 feature 直接引用实现层

❌ `features/A` import `features/B/data/*`
❌ `features/A` import `features/B/application/*`

### 1.3 允许跨 feature 的唯一方式

- domain model（共享契约）
- interface（抽象）
- shared service（`core/services`）

## 2. 依赖方向强制

**允许：** `presentation → application → domain → data`，`core → all`，`shared → core only`

**禁止反向：** `domain → application`，`data → presentation`，`core → features`

## 3. UI 规则

必须使用：`AppColors`、`AppSpacing`、`AppRadius`、`AppTextStyles`、`AppDurations`

❌ 禁止写死：`Color(...)`、`fontSize: 14`、`EdgeInsets.all(16)`、`BorderRadius.circular(12)`

UI 只做：渲染 state、触发 action

### 3.1 状态栏留白（强制）

所有**页面级** UI（二级页、Tab 根页、带顶栏的全屏页）顶部必须预留系统状态栏高度，与一级 Tab 页视觉一致。

**标准写法（二级页 + `AppTopBar`）：**

```dart
final statusBarHeight = AppLayout.statusBarHeight(context);

AppTopBar(
  statusBarHeight: statusBarHeight,
  title: '...',
  onBack: AppRouter.pop,
)
```

**规则：**

- ✔ 统一使用 `AppLayout.statusBarHeight(context)`（见 `lib/core/theme/app_layout.dart`）
- ✔ 原生 iOS/Android：无 safe area inset 时回退 `AppSizes.statusBarPlaceholderHeight`（44）
- ✔ Web 桌面预览：无 inset 时为 **0**，不显示 phantom 状态栏空白
- ✔ 移动端浏览器若有刘海 safe area，仍使用真实 inset
- ✔ 使用 `AppTopBar` 时**必须**传入 `statusBarHeight`（含 loading / error 态若展示顶栏）
- ✔ Tab 根页自定义 Hero / Header 时，首行内容顶边 = `statusBarHeight + 设计稿内边距`（见 `profileHeroUserInfoTop` 等 token）
- ❌ 禁止在各页面重复写 `topInset > 0 ? topInset : AppSizes.statusBarPlaceholderHeight`
- ❌ 禁止顶栏/标题/返回钮直接贴屏幕物理顶边（`SafeArea` 单独包裹而不传 `AppTopBar.statusBarHeight` 视为不合规）

**参考实现：** `editor_pick_page.dart`、`account_settings_page.dart`、`membership_page.dart`

### 3.2 弹窗遮罩（强制）

所有 `Dialog` / 居中弹窗弹出后，弹窗背后必须覆盖统一遮罩：**80% 纯黑（`AppColors.overlayScrim80`），不加背景模糊**。

**标准写法：**

```dart
showAppBlurredDialog(
  context: context,
  builder: (_) => YourDialog(...),
)
```

**规则：**

- ✔ 统一使用 `lib/shared/components/app_blurred_dialog.dart` 的 `showAppBlurredDialog`
- ✔ 遮罩色统一 `AppColors.overlayScrim80`（80% 纯黑）
- ✔ 弹窗本体仍按组件层级放置：通用弹窗进 `shared/components`，业务弹窗进 `features/*/presentation/components`
- ✔ 关闭统一用 `Navigator.pop`（弹窗经 `showGeneralDialog` 压入 Navigator）
- ❌ 禁止 feature 内直接调用 `showDialog`
- ❌ 禁止给弹窗遮罩加 `BackdropFilter` 背景模糊

### 3.3 卡片层级（强制）

信息分组卡片内若包含多行列表项，禁止再给每一行套独立卡片背景。

**标准写法：**

- ✔ 外层使用一个 section/card 容器承载标题与内容
- ✔ 内部列表项使用 `Padding` / `Row` / `Column` 渲染
- ✔ 行与行之间使用 `Divider` 或细分割线分隔
- ❌ 禁止 `Card/Container(surfaceCard + radius)` 内再嵌 `Card/Container(surfaceCard + radius)`
- ❌ 禁止为了区分列表项重复叠加圆角背景；如需强调单个操作，使用按钮/标签而不是整行卡片

### 3.4 Tab 内容左右切换（强制）

同级 Tab（如 `书架 / 阅读历史`、消息页 Tab、记录页 Tab）支持左右滑动时，必须采用首页同款跟手过渡：**顶部 Tab / Header 固定不动，仅 Tab 下方内容区域横向滑动切换**。

**标准写法：**

```dart
AppSwipeTabSwitcher(
  selectedIndex: selectedIndex,
  onIndexChanged: (index) => cubit.switchTab(tabs[index]),
  children: [
    FirstTabBody(),
    SecondTabBody(),
  ],
)
```

**规则：**

- ✔ 优先使用 `lib/shared/components/app_swipe_tab_switcher.dart`
- ✔ 有固定高度/Expanded 约束的内容区传 `children`，使用 PageView 跟手切换
- ✔ 顶部 Tab、页面 Header、管理按钮等 chrome 必须放在 `AppSwipeTabSwitcher` 外层，保持固定
- ✔ 每个 Tab 内容若包含纵向滚动，应使用独立 `ScrollController`
- ✔ 自适应高度内容（如详情页正文内嵌 Tab）可传 `child + tabCount`，仅启用左右滑动手势，避免无界高度 PageView
- ❌ 禁止只监听 `onHorizontalDragEnd` 后直接切 state 造成硬切
- ❌ 禁止把整个页面（含顶部 Tab/Header）放进 PageView，导致整页滑走

### 3.5 Tab 选中指示器动效（强制）

横向 Tab 底部黄色选中线条需要位移动效时，必须采用统一的“平移 + 宽度拉伸回弹”效果：线条位置平滑移动，移动初期先拉长，随后快速恢复默认长度。

**标准写法：**

```dart
Stack(
  children: [
    Row(children: tabItems),
    ElasticTabIndicator(
      selectedIndex: selectedIndex,
      slotWidth: slotWidth,
      slotPitch: slotWidth + AppSpacing.md,
    ),
  ],
)
```

**规则：**

- ✔ 统一使用 `lib/shared/components/elastic_tab_indicator.dart`
- ✔ Tab item 文案可独立变色；指示器必须是单独一层，不要每个 tab 自己画线
- ✔ `slotWidth` / `slotPitch` 必须与 tab item 布局一致，避免指示器中心点偏移
- ❌ 禁止使用多个透明/显示切换的 `Container` 模拟选中线条
- ❌ 禁止给线条位置使用弹性回弹曲线；弹性只作用于宽度拉伸恢复

## 4. 组件分级

| Level | 位置 | 说明 |
|-------|------|------|
| L1 Primitive | `shared/widgets/` | Button, Input, Text — 可跨项目复用 |
| L2 Composite | `shared/components/` | FormSection, CardLayout — 多 L1 组合 |
| L3 Feature | `features/*/presentation/components/` | 仅当前 feature |

❌ feature 组件进 shared；shared 写业务逻辑；L3 无判断提升到 shared

## 5. 状态管理

- state 只能在 `application` 层创建，UI 禁止创建 state
- 分类：UI State / Domain State / Interaction State，必须分离
- state 不可变、可重建，禁止直接修改字段

## 6. data 层

只做：API request、DTO mapping、cache/storage

❌ 禁止：业务逻辑、UI 结构、状态管理

## 7. domain 层

纯 Dart。❌ 禁止 `import flutter`、`import ui`、`import http`

✔ 只允许：entity、value object、business rule、interface

## 8. shared 污染防控

✔ 高频复用（≥3次）、UI 基础组件、layout

❌ feature 逻辑组件、API 相关组件、页面拆分组件

## 9. 路由

❌ `Navigator.push()` — 必须 `AppRouter.go()` / `AppRouter.pushNamed()`，注册在 `routes/app_router.dart`

## 10. import 规则

优先级：`core → shared → feature → local`

❌ deep import — 必须 `import features/a/index.dart`（如已暴露）

## 11. 代码拆分

必须拆分：widget > 150 行、file > 300 行、nesting > 5 层

## 12. 性能

list 必须 builder；避免全局 rebuild；state 局部订阅；大 widget memoize / const

## 13. Cursor 生成代码前必须判断

1. 属于哪一层
2. 是否违反依赖方向
3. 是否可复用 shared
4. 是否写死样式
5. 是否引入跨 feature 依赖
6. **页面顶部是否已预留状态栏高度**（§3.1，含 Web placeholder 回退）

## 14. 强制重构触发

- UI 包含逻辑
- shared 被污染
- feature 互相引用实现层
- state 跨层修改
- widget 过重不可拆

## 15. 修改后合规审计（强制）

每次完成 `lib/` 下 Dart 代码修改后，必须执行 `.cursor/skills/flutter-post-edit-audit` 并输出审计报告。

修改 `design-system/` 下任意文件后，同样必须执行该 skill：核对 §0.1 三处一致，并运行 `scripts/sync-canvas.sh` 将可视化源文件同步到 Cursor 托管渲染副本。
